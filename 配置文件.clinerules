# 千宇宙工作室 AI 协作规则

## 1. 回复方式
* 一律使用中文。
* 回答尽量简短，先说结论，不要长篇解释。
* 用户问“怎么改”时，只给：
  1. 改哪个文件
  2. 改哪个函数 / 哪一段
  3. 这一段要怎么改
* 用户要改代码时，默认只给“修改的部分”，不要再贴整份文件。
* 不要重复输出前面已经说过的大段背景。

## 2. 代码处理规则
* 默认**不要直接改代码**。
* 默认只做分析，并告诉用户：
  1. 文件路径
  2. 可搜索到的原代码标记
  3. 需要替换成什么
* 只有用户明确要求“你直接改”时，才允许实际修改文件。
* 如果用户要代码修改方案，必须明确分成：
  1. 被替换的代码
  2. 替换成的代码
* 不要只说“改成这样”，必须让用户能直接复制替换。
* 给出的定位信息必须尽量能让用户直接搜索到，优先提供：
  * 函数名
  * 完整语句
  * 附近可搜索的原文
* 不要只给抽象示例，避免用户搜索不到。

## 3. 需求确认规则
* 如果用户是要“排查原因”，就不要直接进入改代码方案，先说原因。
* 如果用户是要“怎么改”，直接给修改点，不要展开无关分析。
* 如果一个问题有多个改法，优先只给最小改动方案。

## 4. 背包与数量概念不得混淆
* 必须明确区分：
  * 玩家怪物背包容量
  * 玩家当前携带 / 单次拿取怪物数量
  * Roblox 默认 Backpack / Tool 栏容量表现
* 回答前先确认自己没有把这三个概念混用。

## 5. Roblox 项目约束
* 不要尝试直接修改 Roblox Studio 编辑器内部脚本。
* 不要使用 `execute_luau` 直接写脚本。
* 不要在终端里运行 Roblox 脚本。
* 优先基于现有 `ServerScriptService`、`StarterPlayerScripts`、`ReplicatedStorage` 结构分析。

## 6. 输出格式
* 优先使用下面这种短格式：

### 要改位置
* `路径`

### 1）服务端改这里 / 2）客户端改这里
* `路径`

### 被替换的代码
```lua
原代码
```

### 替换成的代码
```lua
新代码
```

* 如果有多处修改，就按 1）、2）、3）这样往下列。
* 除非用户明确要求，否则不要再写“完整参考代码”“相关片段”“整份文件”。
* 除非用户明确要求，否则不要一次贴太多代码。

## 7. 规则优先级
* 本文件用于规范回答风格与协作方式。
* 如果系统、开发者、平台规则与本文件冲突，以上层规则为准。
* 在不冲突的前提下，优先遵守本文件。